---
title: Time-Travel Debugging
description: The Event Recorder is an advanced diagnostic tool that captures raw state mutations and events into a zero-allocation stream, allowing you to replay crashes with exact millisecond precision.
navigation:
  icon: i-lucide-history
seo:
  title: Time-Travel Debugging
  description: The Event Recorder is an advanced diagnostic tool that captures raw state mutations and events into a zero-allocation stream, allowing you to replay crashes with exact millisecond precision.
---

## What is Time-Travel Debugging?

When building complex Terminal UIs, figuring out why a layout broke or why the application crashed often requires reconstructing the sequence of keyboard inputs and background events.

Tako provides an **Event Recorder** that safely serializes every action in your application to a `JSONL` trace file on disk. You can then "Time-Travel" by replaying the exact sequence of events, in chronological order and with original timing delays, to watch the system fail right in front of you.

## Recording a Trace

The Recorder operates with zero-allocation streaming, ensuring your application doesn't run out of memory during a long debug session. It also supports **Field Masking** to securely redact passwords or secrets before writing them to disk.

### 1. Enable Recording

You can initialize the Recorder during the bootstrap process or globally:

```go [main.go]
import "github.com/gettako/tako/internal/recorder"

// Force enable recording
os.Setenv("TAKO_RECORD_TRACE", "1")

rec := recorder.New(bus).
    MaskFields("password", "secret", "cvv") // Redacts sensitive data

if err := rec.Start(context.Background()); err != nil {
    panic("Failed to start recorder")
}
defer rec.Stop()
```

By default, if the application is running in Development Mode (e.g., via `go run` or inside a git project), traces will be stored locally in `.tako/traces`.
If it's running in Production (compiled binary), they will be stored in your User Home Directory (e.g., `~/.tako/traces`).

## Replaying a Trace

Once you have a trace file, you can feed it back into the framework using the built-in `replay` CLI command.

```bash
tako replay .tako/traces/trace-1780000000.jsonl
```

Or, if using `go run`:

```bash
go run main.go replay .tako/traces/trace-1780000000.jsonl
```

### What Happens During Replay?

1. **Exact Timing:** The replay tool reads the millisecond timestamp on each event. If Event B happened 800ms after Event A, the replay tool will sleep for exactly 800ms.
2. **State Mutability:** It directly overwrites `ctx.State()` variables and dispatches bus events identically to how they happened live.
3. **Crash Reproduction:** If a certain state configuration triggered a `panic()` in your UI components, the exact same state configuration will recreate the panic during replay, allowing you to attach a debugger or print statements safely.

> [!TIP]
> The Event Recorder pairs wonderfully with the [Watcher](./02.watcher.md). While the Watcher gives you real-time insight, the Recorder acts as a "black box flight recorder" for post-mortem analysis.
